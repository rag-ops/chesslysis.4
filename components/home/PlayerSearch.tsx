"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
export default function PlayerSearch(){const router=useRouter();const [username,setUsername]=useState("");const [busy,setBusy]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const value=username.trim();if(!value)return;setBusy(true);router.push(`/dashboard/${encodeURIComponent(value)}`);}return <form onSubmit={submit} className="mt-8 flex w-full max-w-2xl gap-3"><input value={username} onChange={e=>setUsername(e.target.value)} placeholder="Enter a Chess.com username" className="min-w-0 flex-1 border border-[var(--line)] bg-[var(--surface-strong)] px-4 py-3 text-sm text-[var(--ink)] outline-none placeholder:text-[var(--muted)] focus:border-[var(--accent)]" aria-label="Chess.com username"/><button disabled={busy||!username.trim()} className="ch-btn-primary px-5 py-3 text-sm font-bold disabled:cursor-not-allowed disabled:opacity-50">{busy?"Opening…":"Open dashboard →"}</button></form>}
