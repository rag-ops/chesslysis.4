import { NextResponse } from "next/server"; import { getTrainingPlan } from "@/lib/training/recommendations"; import { errorMessage } from "@/lib/api/errors"; export const runtime="nodejs"; export const dynamic="force-dynamic";
export async function GET(_r:Request,{params}:{params:Promise<{username:string}>}){const{username}=await params;try{const data=await getTrainingPlan(username);if(!data)return NextResponse.json({error:"Player not imported yet",code:"PLAYER_NOT_IMPORTED"},{status:404});return NextResponse.json(data,{headers:{"Cache-Control":"no-store"}})}catch(e){console.error(e);return NextResponse.json({error:"Training recommendations are temporarily unavailable",detail:errorMessage(e),code:"TRAINING_UNAVAILABLE"},{status:503})}}
